const expect = require('chai').expect







describe("button works", function () {
  it("makes sure the button works", function () {
     expect(myFunc.getLast(array)).to.deep.equal("false");
  })
})



describe("toggle start stop", function () {
  it("makes sure the button works", function () {
     expect(myFunc.getLast(array)).to.deep.equal("fal");
  })
})
